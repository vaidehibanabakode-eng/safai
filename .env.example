# ─── OpenAI (GPT-4o Vision — photo analysis) ────────────────────────────────
OPENAI_API_KEY=sk-...

# ─── Google Gemini (text-based complaint categorisation) ─────────────────────
GEMINI_API_KEY=...

# ─── Google Cloud (Speech-to-Text transcription) ─────────────────────────────
GOOGLE_CLOUD_API_KEY=...

# ─── Firebase Admin (push notifications via FCM) ─────────────────────────────
# Paste the full JSON from Firebase Console → Project Settings → Service Accounts
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
